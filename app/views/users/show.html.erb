<%= link_to "Back to Overview", users_path, class: "btn btn-light" %>
<h2><%= @user.name %></h2>
<ul style="list-style-type: none;">

<% @user.trainingsplans.each do |trainingsplan| %>
  <li>
    <%= link_to trainingsplan.name, user_trainingsplan_path(@user) %> for  <%= trainingsplan.goal %>
    <%= link_to "Edit Trainingsplan", edit_user_trainingsplan_path(@user)   %>
  </li>
  <% end  %>
</ul>




  <%= link_to 'Edit User', edit_user_path(@user), class: "btn btn-secondary btn-outline-warning" %>
  <%= link_to 'Delete', user_path(@user), class: "btn btn-secondary btn-outline-danger",
      method: :delete,
      data: { confirm: "Are you sure"} %>
</div>